# DogePay Production Stripe Billing (billingplane 15 rules enforced)
# Server-only: NEVER commit real keys. Use sk_live_ for prod (no test).
# Blink or platform env for secrets.
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
# Price IDs from Stripe Dashboard (no hardcode in code)
STRIPE_PRICE_PRO_MONTHLY=price_...
# For usage metered (in-arrear via invoice.created)
# DB/Supabase or Blink backend recommended for real entitlements (localStorage v1 only)
